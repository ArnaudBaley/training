console.log('coucou');

// Generate a 2048 board 
const generateBoard = (width, height) => {
    const result = [];
    for (let i=0; i<height; i++) {
        const row = [];
        for (let j=0; j<width; i++) {
            row.push()
        }        
    }
}

const getRandomFiboNumber = () => {
    const firstFiboSequence = [1, 2, 3, 5, 8, 13];
    return firstFiboSequence[Math.floor(firstFiboSequence.length)];
}

console.log(getRandomFiboNumber());
